# GROUP-002 fixture：list 分组键按**整组键列表**成组（oracle ⑦ 定案 2026-08-02）、
# link 标量键、空 list 键。（P2b 时期本文件经历过「暂定拒绝」→「扇出」→「整组键列表」三轮。）
views:
  # 整组键列表基本形：只有 Tagger 有 tags: [x, y] → 一组键 ["x","y"]；其余 5 篇缺失 → null 组。
  - type: table
    name: byTags
    order:
      - file.name
      - tags
    groupBy:
      property: tags
      direction: ASC
  # 整组键列表 + 跨行重叠：list(status, area) 每行一个完整键，两行同键才同组。
  - type: table
    name: byMulti
    order:
      - file.name
    groupBy:
      property: list(status, area)
      direction: ASC
  # 行内重复元素：list(status, status) 去重后键 [status]。
  - type: table
    name: byDup
    order:
      - file.name
    groupBy:
      property: list(status, status)
      direction: ASC
  # 空 list 键：成 [] 组（oracle ⑦：官方 file.tags 缺失即空列表），不静默丢行。
  - type: table
    name: byEmpty
    order:
      - file.name
    groupBy:
      property: list()
      direction: ASC
  # link 是**标量**键（不是多值）：按路径感知相等分组，组序按归一 path。
  - type: table
    name: byLink
    order:
      - file.name
    groupBy:
      property: link(status)
      direction: ASC
