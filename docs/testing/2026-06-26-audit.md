# 现状体检报告（x-basalt · 2026-06-26）

> 目的：把「黑盒感」转成可追溯清单。每条带 `文件:行号`、严重度、**复核状态**、是否有注释承认。
> 关联：依赖决策 [`../specs/2026-06-26-deps-build-vs-buy.md`](../specs/2026-06-26-deps-build-vs-buy.md)、覆盖矩阵 [`../specs/2026-06-26-coverage-matrix.md`](../specs/2026-06-26-coverage-matrix.md)

## 复核状态图例

- **✅ 复核**：本次会话直接读源码确认。
- **⚠️ 待复现**：模块审计提出，本次未逐行复核，修前需各自复现。
- **✗ 已下调/推翻**：经复核认为不成立或被高估，记录以免误导。

## 0. 验证基线（本次实测）

| 门禁 | 结果 |
|---|---|
| `tsc --noEmit`（typecheck） | ✅ exit 0 |
| `pnpm test`（node:test） | ✅ 52 pass / 0 fail / 0 todo |
| `oxlint` | ✅ 0 告警 |
| `oxfmt --check .` | ❌ 18 个 docs/json 文件红（中文 prose 漂移，**无 `.ts`**）——既有基线问题 |
| `src/**` 死依赖 import 数 | `unified`=0 `remark-parse`=0 `@flowershow/remark-wiki-link`=0 `zod`=0 |

**注意：52 个测试几乎全是正路径/主路径**，绿灯不代表规范覆盖；这是「黑盒感」的直接来源（见覆盖矩阵）。

## 1. parser

| # | 位置 | 严重度 | 复核 | 描述 | 注释承认 |
|---|---|---|---|---|---|
| P1 | `parser/index.ts:217` | 中 | ✅ | `extractWikilinks(body)` 用**原始**正文，不掩码代码块；而 tag/highlight 用 `masked`。围栏代码块内 `[[link]]` 会被误提取，标准不一致 | 无 |
| P2 | `parser/index.ts:219-222` | 低 | ✅ | task/blockRef/callout 用原始 `lines`，不剔代码块 | 有（:213「列为后续」） |
| P3 | `types.ts:19` + `index.ts:138-148` | 中 | ✅ | task 节点无 `due_date`，parser 不提取；但 `schema.ts:63`/`sql-generator.ts:78` 引用该列 → 恒为 NULL。调研 §2 line 58 明确要求 | 无 |
| P4 | `types.ts:15` + `index.ts:187` | 低 | ✅ | callout `foldable:boolean` 把 `+`(默认展开)/`-`(默认折叠) 合并，丢默认态语义 | 无 |
| P5 | `wikilink.ts` / `utils/path.ts` `linkKey` | 中 | ⚠️ | basename 去重/解析：`[[A/Note]]` 与 `[[B/Note]]` 同名会误合并（同时污染索引反链） | 有（sql-gen:64、schema:23 承认 MVP 近似） |
| P6 | 全局 | 低 | ⚠️ | HTML 注释 `<!-- -->` 内语法未剔除 | 无 |
| P7 | `parser/index.ts:167-193` | — | ✗ | 「相邻 callout 被吞噬」**已下调**：Obsidian 同样把连续 `>` 行并为一个 blockquote，仅在两 callout 间**无空行**时合并，属边界存疑而非明确 bug | n/a |
| P8 | `parser/index.ts:85-110` | — | ✗ | 「maskCode 对 CRLF 输出变短」**推翻**：`\r` 被 `blankNonNewline` 替为空格，长度保留 | n/a |

## 2. query

| # | 位置 | 严重度 | 复核 | 描述 |
|---|---|---|---|---|
| Q1 | `sql-generator.ts:157-163` | 中 | ✅ | LIKE 通配符未转义：`%${arg}%` 中 `arg` 含 `%`/`_` 被当通配符，`contains(title,"50%")` 语义错；且无 `ESCAPE` |
| Q2 | `sql-generator.ts:135-155` | 中 | ✅ | `icontains` 对 `file.tags/inlinks/outlinks` 与 `contains` 生成相同 SQL（大小写敏感），承诺语义未兑现；标量分支(:157)才用 `LOWER()` |
| Q3 | `sql-generator.ts:212-213` | 中 | ✅ | TABLE 恒先 `addCol("file.name")` 再遍历字段，`TABLE file.name, ...` 产生**重复列** |
| Q4 | `sql-generator.ts:84` | 中 | ✅ | 未知字段抛裸 `Error` 而非 `DqlSyntaxError`，`instanceof` 过滤会漏；且丢失位置 |
| Q5 | `sql-generator.ts:218-220` | 低 | ✅ | `SORT file.tags`（JSON 聚合列）按 JSON 串排序，无意义但不报错 |
| Q6 | `sql-generator.ts:164` | 低 | ✅ | `throw 不支持的函数` 不可达（fn 已类型穷举），缺 `never` 断言 |
| Q7 | `sql-generator.ts:222` | 低 | ✅ | `LIMIT -5` 不校验，SQLite 视为无限制 |
| Q8 | `query/index.ts` REGEXP 注册 | 低 | ⚠️ | 自定义 REGEXP 无 ReDoS 防护（`(a+)+b` 灾难回溯阻塞查询线程） |
| Q9 | 子集缺口 | 中 | ✅ | 无 `GROUP BY`/`FLATTEN`/`TASK`/`CALENDAR`/`WITHOUT ID`/多键 SORT（详见覆盖矩阵） |

> 安全面正向：所有用户值走 `?` 占位绑定，frontmatter 字段名有 `/^[A-Za-z0-9_]+$/` 白名单（`sql-generator.ts:83`），无 SQL 注入。

## 3. indexer

| # | 位置 | 严重度 | 复核 | 描述 |
|---|---|---|---|---|
| I1 | `watcher.ts:37-56` | 中/高 | ✅ | `startWatch` 无 `watcher.on("error")`；chokidar error 事件无处理器 → 可能崩进程 |
| I2 | `indexer/index.ts` onUnlink | 中 | ⚠️ | onUnlink 同步 `remove` 抛错会中断监听（onWrite 有 `.catch`，不对称） |
| I3 | `indexer/index.ts` rebuild | 中 | ⚠️ | rebuild 把所有文件内容堆内存后再写事务（大库 OOM）；`buildPayload` 串行 await（慢） |
| I4 | rebuild × watch | 中 | ⚠️ | 二者可并发；watch 的 update 先提交、rebuild 大事务后覆盖 → 索引静默回退 |
| I5 | `utils/path.ts` `linkKey` + `sql-generator.ts:64-67` | 中 | ✅ | basename 反链歧义：同名异目录文件 `name_key` 相同，inlinks JOIN 互相串味 |
| I6 | `indexer/index.ts` ctime | 低 | ⚠️ | Linux `birthtimeMs` 为 0 回退 `ctimeMs`（元数据变更时间），`file.ctime` 跨平台语义不一 |

## 4. cli / config / skill

| # | 位置 | 严重度 | 复核 | 描述 |
|---|---|---|---|---|
| C1 | 无 `tests/cli.test.ts` | 高 | ✅ | CLI 五命令、优先级链、退出码、YAML 序列化、on-change 替换全无端到端测试 |
| C2 | `cli.ts:126,144,150` vs `:90` | 中 | ✅ | **`--format` 仅 `parse` 接线**；`query`/`skill recall`/`skill list` 调 `emit()` 不传 format → 恒 JSON，`config.format` 对它们无效（比预想更窄） |
| C3 | `cli.ts:170-171` | 中 | ✅ | `exec(onChange.replaceAll("{file}", file))` 把文件路径拼进 shell 串，含空格/`$`/`;`/反引号会断词或注入 |
| C4 | `config.ts:81` | 低/中 | ✅ | YAML 配置被包成 `---\n${raw}\n---\n`；若内容以 `---` 开头或含 `---` 行，gray-matter 提前闭合 → 静默返回 `{}` |
| C5 | `cli.ts:53` | 低 | ✅ | `toYaml` 对象键裸输出，含空格/`:` 的 frontmatter 键产出非法 YAML（值侧已 `JSON.stringify` 保护） |
| C6 | `cli.ts:62` | 低 | ✅ | `emit` 对非法 `--format`（如 `xml`）静默退化 JSON，不报错/不退出码 1 |
| C7 | `cli.ts:181` | 低 | ✅ | 顶层 catch `(err as Error).message`：抛非 Error 时输出 `✗ undefined` |
| C8 | 全局 | 低 | ✅ | AGENTS.md「日志脱敏」无 `redact`/`mask` 实现（但本项目无网络/敏感数据，风险低） |

## 5. 完成度评估汇总

| 模块 | 主路径 | 规范/子集覆盖 | 主要短板 |
|---|---|---|---|
| parser | 可跑 | ~60% | 代码块掩码不一致、due_date 缺、HTML 注释、basename 歧义 |
| query | 可跑 | ~70% | LIKE 转义/icontains 语义/重复列；缺 GROUP BY/FLATTEN/TASK |
| indexer | 可跑 | ~70% | 监听健壮性（无 error 处理）、大库内存/竞态、basename 反链 |
| cli/config | 可跑 | ~85% | 零端到端测试、--format 仅 parse、on-change 注入 |

**总判断**：工程门面（typecheck/lint/测试绿、ESM/TS 严格、CLI 接线）≈ 中上；核心算法与规范落地 ≈ 原型级。最能当亮点的「DQL 编译器 + 解析器」恰是现在最薄、bug 最集中的部分。

> 本报告只诊断、不改码。修复项的取舍与排期见后续计划。
