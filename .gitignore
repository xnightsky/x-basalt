# ============ 操作系统 ============
# macOS
.DS_Store
.AppleDouble
.LSOverride
Icon
._*

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/
*.lnk

# ============ IDE / 编辑器 ============
.idea/
.vscode/
*.swp
*.swo
*~
.project
.classpath
.settings/

# ============ Node.js ============
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*
.pnpm-debug.log*

# 依赖锁文件：保留 pnpm-lock.yaml 入仓

# ============ 构建输出 ============
dist/
build/
out/
.cache/
*.tsbuildinfo

# ============ 测试 & 覆盖率 ============
coverage/
.nyc_output/
*.lcov
test-results/

# ============ 环境 & 凭证 ============
.env
.env*
.env.*.local
*.pem
*.key
*.p12
*.pfx
secrets/
credentials/
secrets.json
credentials.json

# ============ 本地工作树 ============
.worktrees/

# ============ 日志 ============
*.log
logs/

# ============ 数据目录 ============
data/

# ============ 临时文件 ============
tmp/
temp/
*.tmp
*.temp
.tmp/
.tmp-*/

# ============ SQLite 索引产物（运行期生成，不入仓）============
*.db
*.db-journal
*.db-wal
*.db-shm
index.db*

# ============ 本地项目配置 / 产物：默认放仓库内隐藏目录 .x-basalt/（不入仓，仅保留示例）============
.x-basalt/*
!.x-basalt/config.example.yaml
# 扁平式配置（也支持，但非默认）
.x-basalt.yaml
.x-basalt.yml
.x-basalt.json5
.x-basalt.json

# =========== AI ============
# 本地 AGENTS 规则（不入仓）；示例模板 AGENTS.local.example.md 入仓
AGENTS.local.md
.claude/settings.json
.claude/settings.local.json
.claude/skills/
.claude/commands/
.agents/skills/
